module fibonacci;

fn fib(var i32 input) i32 {
  if input <= 1 {
    return 1;
  }
  return fib(input - 2) + fib(input - 1);
}

extern fn printf(var string t var i32 n1) void;

fn main() void {
  printf("%d\n" fib(10));
  return;
}
