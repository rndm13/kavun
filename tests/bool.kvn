module bool;

extern fn printf(string text) void;

fn main() void {
  var bool flag = false;
  if flag {
    printf("flag is set to true\n");
  }
  if !flag {
    printf("flag is set to false\n");
  }
  return;
}
